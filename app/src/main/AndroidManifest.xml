<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tvm.tvm">

    <!--网络-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--外存-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!--相机-->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".application.AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity"></activity>
        <!--<activity android:name=".activity.MainActivity"></activity>-->
        <activity android:name=".activity.SettingsActivity"></activity>
        <activity android:name=".activity.UserListActivity"></activity>
        <activity android:name=".activity.ChangePasswordActivity"></activity>
        <activity android:name=".activity.NormalSettingActivity"></activity>
        <activity android:name=".activity.BillQueryActivity"></activity>
        <activity android:name=".activity.PriceListActivity"></activity>
        <activity android:name=".activity.PriceEditActivity"></activity>
        <activity android:name=".activity.SelectPriceActivity"></activity>
        <activity android:name=".activity.PayDetailActivity"></activity>
    </application>

</manifest>